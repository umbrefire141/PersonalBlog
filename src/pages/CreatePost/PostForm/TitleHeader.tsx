import { CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

const TitleHeader = () => {
	return (
		<CardHeader>
			<CardTitle>New Post</CardTitle>
			<CardDescription>Share your latest work with the world</CardDescription>
		</CardHeader>
	);
};

export default TitleHeader;
