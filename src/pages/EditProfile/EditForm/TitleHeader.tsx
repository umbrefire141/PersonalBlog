import { CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

const TitleHeader = () => {
	return (
		<CardHeader>
			<CardTitle>Profile Info</CardTitle>
			<CardDescription>Update your bio, title, and description</CardDescription>
		</CardHeader>
	);
};

export default TitleHeader;
