import { CardDescription, CardHeader, CardTitle } from '@/components/ui/card';

const TitleHeader = () => {
	return (
		<CardHeader>
			<CardTitle>Sign In</CardTitle>
			<CardDescription>Sign in to manage your creative studio</CardDescription>
		</CardHeader>
	);
};

export default TitleHeader;
